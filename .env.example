# Required for account sessions (use any long random string locally)
SESSION_SECRET=change-me-to-a-long-random-string

# Optional: override local data store location (defaults to ./data/vault-tracker-store.json)
# DATABASE_PATH=./data/vault-tracker-store.json
